export {};  // Make this a module

// Comment out HMR code for now
/*
if (import.meta.hot) {
    import.meta.hot.accept(['../types/types.ts', '../utils/settings.ts'], (modules) => {
        console.log('Types or settings updated, reloading...');
        window.location.reload();
    });
}
*/
